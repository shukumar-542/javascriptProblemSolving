var num  = 2.59;
var result = Math.floor(num);
var celling = Math.ceil(num);

for(i=1; i<10; i++){
      var random = Math.random()*6;
      var dice = Math.round(random);
      console.log(dice);
}

